% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preferential-sampling.R
\name{preferential_sampling}
\alias{preferential_sampling}
\title{Preferential-sampling specification}
\usage{
preferential_sampling(
  formula,
  data,
  re_form_iid = NA,
  offset = 0,
  coefficient = "constant",
  spatial = c("off", "on")
)
}
\arguments{
\item{formula}{A two-sided formula for the sampling model, e.g.
\code{sampled ~ 0 + factor(year) + distance_to_port}. The response column
must be 0 (eligible but not sampled), 1 (sampled), or \code{NA} (unknown).
These coefficients belong to the sampling model alone. Only ordinary
fixed effects are supported: no smoothers, random effects, or \code{offset()}
terms.}

\item{data}{A data frame with one row per eligible cell and time step. It
must contain the mesh coordinates, the \code{time} column of the main model,
the sampling response and covariates, and every covariate in the main
model's fixed-effect formula. The main model is evaluated on these rows
as with \code{newdata} in \code{\link[=predict.sdmTMB]{predict.sdmTMB()}}, so set catchability covariates
(e.g., gear) to reference values here.}

\item{re_form_iid}{\code{NA} (default) excludes the main model's IID random
effects from the expected-catch surface. \code{NULL} would include them, but
this is not supported yet for models with IID random effects.}

\item{offset}{Offset for the main model's expected-catch surface on the
link scale (not an offset for the sampling model). A single value or one
value per row of \code{data}. The default \code{0} means unit exposure.
Observation offsets are not copied.}

\item{coefficient}{How the preference coefficient varies. Only
\code{"constant"} (one coefficient) is currently supported.}

\item{spatial}{Whether to add a time-invariant spatial random field to the
sampling model (\code{"off"} or \code{"on"}). This does not affect the main
model's fields.}
}
\value{
A list of class \code{sdmTMB_preferential}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Specify a model for which cells of an eligible sampling frame were sampled
in each time step, linked to the main model's standardized expected catch.
Pass the result to the \code{preferential} argument of \code{\link[=sdmTMB]{sdmTMB()}}.

\strong{The joint likelihood is not implemented yet.} This constructor and the
preparation of its inputs are in place, but \code{\link[=sdmTMB]{sdmTMB()}} currently stops
before fitting a model with a \code{preferential} specification.
}
\examples{
grid <- data.frame(
  X = c(0, 1, 0, 1), Y = c(0, 0, 1, 1), year = 2020,
  sampled = c(1, 0, NA, 1), depth = 100
)
preferential_sampling(sampled ~ 1, data = grid)
}
