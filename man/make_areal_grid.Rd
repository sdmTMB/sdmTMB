% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/areal.R
\name{make_areal_grid}
\alias{make_areal_grid}
\title{Create an areal grid domain by overlaying point data}
\usage{
make_areal_grid(
  data,
  xy_cols,
  spatial_domain = NULL,
  cellsize = NULL,
  n = NULL,
  square = TRUE,
  space_column = "grid_cell",
  adjacency = c("rook", "queen"),
  crs = NA
)
}
\arguments{
\item{data}{A data frame containing point coordinates.}

\item{xy_cols}{Character vector of length 2 naming spatial coordinate columns
in \code{data}.}

\item{spatial_domain}{Optional \code{sf}/\code{sfc} polygon object. If \code{cellsize} or
\code{n} is supplied, this object is treated as a boundary from which a grid is
generated and clipped. Otherwise it is treated as the grid itself.}

\item{cellsize}{Optional cell size passed to \code{\link[sf:st_make_grid]{sf::st_make_grid()}}.}

\item{n}{Optional grid dimensions passed to \code{\link[sf:st_make_grid]{sf::st_make_grid()}}. Ignored if
\code{cellsize} is supplied.}

\item{square}{Logical passed to \code{\link[sf:st_make_grid]{sf::st_make_grid()}}. Use \code{FALSE} for
hexagonal grids.}

\item{space_column}{Column name to add to returned data.}

\item{adjacency}{Polygon adjacency type: \code{"rook"} for shared edges or
\code{"queen"} for any touching boundary.}

\item{crs}{Optional coordinate reference system for \code{data} coordinates,
passed to \code{\link[sf:st_as_sf]{sf::st_as_sf()}}.}
}
\value{
A list with \code{data}, \code{grid}, and \code{domain} elements. \code{domain} can be
supplied to \code{\link[=sdmTMB]{sdmTMB()}} via the \code{mesh} argument.
}
\description{
Build or use an \code{sf} polygon grid, overlay point observations onto grid
cells, construct grid-cell adjacency, and return labelled data plus an
\code{sdmTMBareal} domain for SAR/CAR models.
}
\examples{
\dontshow{if (require("sf", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dat <- data.frame(
  x = c(0.25, 1.25, 0.25, 1.25),
  y = c(0.25, 0.25, 1.25, 1.25),
  depth = c(12, 18, 9, 15)
)

boundary <- sf::st_as_sf(
  sf::st_as_sfc(sf::st_bbox(c(xmin = 0, ymin = 0, xmax = 2, ymax = 2)))
)

areal_grid <- make_areal_grid(
  data = dat,
  xy_cols = c("x", "y"),
  spatial_domain = boundary,
  n = c(2, 2)
)

head(areal_grid$data)
areal_grid$domain$n_s
\dontshow{\}) # examplesIf}
}
