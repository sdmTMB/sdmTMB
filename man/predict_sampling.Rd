% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preferential-sampling.R
\name{predict_sampling}
\alias{predict_sampling}
\title{Predict sampling probabilities from a preferential-sampling model}
\usage{
predict_sampling(object, type = c("response", "link"), nsim = 0)
}
\arguments{
\item{object}{A model fit by \code{\link[=sdmTMB]{sdmTMB()}} with \code{preferential}.}

\item{type}{\code{"response"} for sampling probabilities or \code{"link"} for the
logit scale. Applies to \code{est} and to the draws with \code{nsim > 0}; the
predictor pieces are always on the logit scale.}

\item{nsim}{If \verb{> 0}, return a matrix of \code{nsim} draws (columns) of \code{est}
for each frame row (rows), from the joint precision matrix of all fixed
and random effects. Use these draws for uncertainty intervals.}
}
\value{
With \code{nsim = 0}, the sampling \code{data} in its original row order with these
columns added:
\itemize{
\item \code{est}: the sampling probability (or its logit, for \code{type = "link"}).
\item \code{est_target}: the shared target \eqn{h}, the main model's log
standardized expected catch.
\item \code{est_fixed}: the sampling formula's contribution \eqn{Z\gamma}.
\item \code{est_baseline}: the baseline deviation \eqn{\alpha_t}, if \code{baseline}
was used.
\item \code{est_preference}: the preference contribution, \eqn{b h} plus
\eqn{(b_t - b)(h - \bar{h}_t)} for a temporal \code{coefficient}.
\item \code{est_xi}: the sampling field \eqn{\xi}, if estimated.
}

On the link scale, \code{est} is the sum of \code{est_fixed}, \code{est_baseline},
\code{est_preference}, and \code{est_xi}. With \code{nsim > 0}, a matrix with one row per frame row.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Fitted sampling probabilities, and their predictor pieces, for the
sampling frame of a model fit with \code{preferential} (see
\code{\link[=preferential_sampling]{preferential_sampling()}}). Predictions for a new sampling frame or new
time steps are not supported yet.
}
