% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zero-one-map.R
\name{make_zero_one_map}
\alias{make_zero_one_map}
\title{Fix factor-level intercepts for groups with all zeros or ones}
\usage{
make_zero_one_map(formula, data, group, family, weights = NULL, value = 20)
}
\arguments{
\item{formula}{The fixed-effect formula to be used in \code{\link[=sdmTMB]{sdmTMB()}}. For delta
models, either a single formula applied to both linear predictors, or a
\code{list()} of two formulas. The formula should contain \code{factor(group)} (or
\code{as.factor(group)}, or a pre-converted factor column) so each level has its
own column, typically \code{response ~ 0 + factor(year)}.}

\item{data}{The data frame passed to \code{\link[=sdmTMB]{sdmTMB()}}.}

\item{group}{Character. Name of the grouping column in \code{data} whose levels
should be inspected for all-zero/all-positive observations. Most commonly
the time column.}

\item{family}{The \code{sdmTMB} family object (e.g., \code{tweedie()},
\code{delta_gamma()}).}

\item{weights}{Optional numeric vector of trial sizes for non-delta binomial
or beta-binomial models fit with proportion data. This should match the
\code{weights} argument passed to \code{\link[=sdmTMB]{sdmTMB()}}. Ignored for other families.}

\item{value}{Positive numeric. Magnitude at which to fix the intercept;
\code{plogis(20)}, \code{plogis(-20)}, and \code{exp(-20)} are all within ~2e-9 of 0 or 1,
which is plenty.}
}
\value{
A named list with elements:
\describe{
\item{\code{map}}{A list with \code{b_j} (and \code{b_j2} for delta families) factors
suitable for \code{sdmTMBcontrol(map = ...)}.}
\item{\code{start}}{A list with \code{b_j} (and \code{b_j2}) starting-value vectors
suitable for \code{sdmTMBcontrol(start = ...)}.}
\item{\code{all_zero_levels}}{Group levels with all-zero observations.}
\item{\code{all_one_levels}}{Group levels with all-positive observations
for delta families, or all-success observations for non-delta
binomial/beta-binomial families. Empty for other families.}
}
}
\description{
A grouping factor level (e.g. factor(year), but also possibly a region,
stratum, region-by-year interaction, or any other factor level) with all-zero
observations can drive the corresponding factor-coded coefficient to \code{-Inf},
causing convergence problems. In delta/hurdle and binomial or beta-binomial
models, an all-positive or all-success level can similarly drive the
encounter coefficient to \code{+Inf}. Tweedie will have the same issue with all zeros.This helper returns \code{map} and \code{start} lists
suitable for passing through \code{\link[=sdmTMBcontrol]{sdmTMBcontrol()}} to fix those problem
coefficients at large positive/negative values. This mirrors an approach
taken in \pkg{VAST}.
}
\details{
Inspect the returned \code{all_zero_levels} and \code{all_one_levels} elements before
fitting to make sure the detected problem levels match your expectation.

Although the motivating use case is years in fisheries index standardization
(e.g., \code{density ~ 0 + factor(year)}), the same mechanism applies to any
factor whose levels each get a column in the design matrix. For an
interaction such as region by year, create a combined column (e.g.,
\code{data$region_year <- interaction(data$region, data$year, drop = TRUE)}) and
use \code{~ 0 + region_year}.

For Poisson-link delta families (e.g., \code{delta_gamma(type = "poisson-link")}),
the first linear predictor controls both the encounter probability and the
positive rate, so fixing it shifts the meaning of the second-linear-predictor
coefficients in those levels. The helper still works for all-zero levels (the
large negative value makes encounter probability ~0, and the level
contributes ~0 to the index), but emits a message for all-positive levels.
Although this affects interpretation of the coefficients, the overall
combined prediction remains OK.
}
\examples{
d <- pcod
d$density[d$year == 2003] <- 0 # force an all-zero year

m <- make_zero_one_map(
  formula = density ~ 0 + factor(year),
  data = d,
  group = "year",
  family = tweedie()
)
m$all_zero_levels
m$all_one_levels
m$start
m$map

\donttest{
mesh <- make_mesh(d, c("X", "Y"), cutoff = 15)
fit <- sdmTMB(
  density ~ 0 + factor(year),
  data = d,
  mesh = mesh,
  time = "year",
  family = tweedie(),
  spatial = "on",
  spatiotemporal = "off", # off for example speed
  control = sdmTMBcontrol(map = m$map, start = m$start) #<
)
fit
coef(fit)
}
}
\seealso{
\code{\link[=sdmTMBcontrol]{sdmTMBcontrol()}}, \code{\link[=sdmTMB]{sdmTMB()}}
}
