% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate-diffusion.R
\name{plot_covariate_diffusion}
\alias{plot_covariate_diffusion}
\title{Plot Covariate Diffusion from a Point on the Mesh}
\usage{
plot_covariate_diffusion(
  object,
  covariate = NULL,
  component,
  time_value = 1,
  n_steps = 3L,
  common_scale = FALSE
)
}
\arguments{
\item{object}{A fitted \code{\link[=sdmTMB]{sdmTMB()}} model with \code{covariate_diffusion}.}

\item{covariate}{Optional covariate name from \code{covariate_diffusion}.
Required when multiple lag covariates were fitted.}

\item{component}{Covariate diffusion component name. Must be exactly one of
\code{"space"}, \code{"time"}, or \code{"combined"}. \code{"combined"}
plots the joint response of all covariate-diffusion components fitted for
\code{covariate}.}

\item{time_value}{Optional time slice for the impulse. Supply either a
modeled time value or a 1-based time index. Defaults to 1.}

\item{n_steps}{Number of transformed slices to plot starting at
\code{time_value}. Defaults to 3.}

\item{common_scale}{Should transformed panels share a common color scale?
Defaults to \code{FALSE}.}
}
\value{
Invisibly returns a list with impulse/transformed fields on vertices,
triangle summaries used for plotting, selected indices, and a \code{ggplot}
object (when \pkg{ggplot2} is available).
}
\description{
Diagnostic visualization of an impulse covariate diffusing through one
covariate-diffusion component (\code{space()} or \code{time()}).
Values are plotted as colored mesh triangles, so no prediction grid is
required.
}
