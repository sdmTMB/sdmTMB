% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributed-lags.R
\name{plot_distributed_lag_diffusion}
\alias{plot_distributed_lag_diffusion}
\title{Plot Distributed-Lag Diffusion from a Point on the Mesh}
\usage{
plot_distributed_lag_diffusion(
  object,
  covariate = NULL,
  component,
  time_value,
  n_steps = 3L,
  common_scale = FALSE,
  plot = TRUE
)
}
\arguments{
\item{object}{A fitted \code{\link[=sdmTMB]{sdmTMB()}} model with \code{distributed_lags}.}

\item{covariate}{Optional covariate name from \code{distributed_lags}.
Required when multiple lag covariates were fitted.}

\item{component}{Distributed-lag component name. Must be exactly one of
\code{"space"}, \code{"time"}, or \code{"spacetime"}.}

\item{time_value}{Time slice for the impulse. Supply either a modeled time
value or a 1-based time index.}

\item{n_steps}{Number of transformed slices to plot starting at
\code{time_value}. Defaults to 3.}

\item{common_scale}{Should transformed panels share a common color scale?
Defaults to \code{FALSE}.}

\item{plot}{Logical. If \code{TRUE} (default), draw the plot.}
}
\value{
Invisibly returns a list with impulse/transformed fields on vertices,
triangle summaries used for plotting, selected indices, and a \code{ggplot}
object (when \pkg{ggplot2} is available).
}
\description{
Diagnostic visualization of an impulse covariate diffusing through one
distributed-lag component (\code{space()}, \code{time()}, or \code{spacetime()}).
Values are plotted as colored mesh triangles, so no prediction grid is
required.
}
